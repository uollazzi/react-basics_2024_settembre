// function somma(n1: number, n2: number): number {
//     let risultato = 0;

//     risultato = n1 + n2;

//     return risultato;
// }

export const somma = (n1: number, n2: number): number => {
    let risultato = 0;

    risultato = n1 + n2;

    return risultato;
}

export class Studente {

}

// const sottrazione = (n1: number, n2: number): number => {
//     return n1 - n2;
// }

const divisione = (n1: number, n2: number): number => {
    return n1 / n2;
}

export default divisione;
